import { atom } from 'recoil';

export const scrappedSpotsState = atom({
  key: 'scrappedSpotsState',
  default: {},
});

export const scrappedCurationsState = atom({
  key: 'scrappedCurationState',
  default: {},
});
